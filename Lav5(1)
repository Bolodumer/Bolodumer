import math
x = float(input('Задайте Х: '))
if x > 9:
    y = 3.11+math.log(2*x,math.e)+math.log(x+8,3)
elif 1 <= x <= 9:
    y = (1/x)+((2+x)/(math.pow(math.e,x)+math.pow(x,2)))
else:
    y = 6.23*(x-0.7+math.pow(math.e,3*x))
print('Відповідь: ',y)
#Написати програму, яка обчислюватиме значення функції у залежності від значення аргументу
