#Варіант 20 Написати програму, яка обчислює значення виразу

import math
def func(x0,x1,x2,x3):
    L = (x0*math.cos(x1)+x2)-((math.tan(x2)+math.pow(math.e,x3))/math.pow(2,x2+2.4))
    return(L)
x=float(input('Ведіть Х: '))
y=float(input('Ведіть Y: '))
z=float(input('Ведіть Z: '))
a=float(input('Ведіть A: '))
L=func(x,y,z,a)
print(L)
